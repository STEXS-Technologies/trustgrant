[package]
name = "kani-proofs"
version = "0.1.0"
edition = "2024"
publish = false

[lints]
workspace = true

[dependencies]
serde_json = "1"
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
